# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
#MV: Not using this anymore because GeoKit doesn't work on SQLite
#development:
#  adapter: sqlite3
#  database: db/development.sqlite3
#  pool: 5
#  timeout: 5000

development:
  adapter: mysql
  encoding: utf8
  database: point2wine_development
  host: localhost
  username: root
  password: beysenc
  
# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.

test:
  adapter: sqlite3
  database: db/test.sqlite3
  pool: 5
  timeout: 5000

production:
  adapter: mysql
  encoding: utf8
  database: barrelrun
  host: localhost
  username: root
  password: beysenc
