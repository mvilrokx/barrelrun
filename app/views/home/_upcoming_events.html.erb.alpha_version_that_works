   <table cellspacing='0' id='top_list'>
   <caption>Upcoming Events <%= link_to_remote (
      image_tag("/images/arrow_refresh.png",:border => 0,:title => "Refresh List"),
      :url=>"/home/upcoming_events",
      :method=>"get",
      :update=>"upcoming_events") %>
   </caption>
   <% @events.each do |event| %>
     <tr>
       <td>
         <table cellspacing='0' width="100%">
           <tr>
           <% if user_signed_in? %>
             <div class="stars">
              <form action="/events/<%= event.id %>/rating/" method="post">
                 <%= render_stars event.average_rating||0 %>
              </form>
             </div>
             <td>
               <div class="action-menu">
                  <a class="action-menu-link" href="#"><span class="action-span">action</span></a>
                  <div style="display: none;" class="list-action-menu">
                    <ul>
                      <li><a href="/events/<%= event.id%>/comments" class="dialog_form_link">Comments</a></li>
              <% if event.favorites.find_by_user_id(current_user) %>
                      <li><a href="/events/<%= event.id%>/favorites/<%= event.favorites.find_by_user_id(current_user).id %>" class="remove_as_favorite">Remove From My Favorites</a></li>
              <% else %>
                      <li><a href="/events/<%= event.id%>/favorites" class="add_as_favorite">Add To My Favorites</a></li>
              <% end %>
                      
                      <li><a href="/events/<%= event.id%>" class="dialog_form_link">Show Details</a></li>
                      <li><a class="map-wine" href="#" data-winery-lat=<%=h event.winery.lat %> data-winery-lng=<%=h event.winery.lng %> >Map It</a></li>
                    </ul>
                  </div>
               </div>
             </td>
           <% else %>
             <div class="stars">
              <form action="/events/<%= event.id %>/rating/" method="post">
                 <%= render_stars event.average_rating||0, true %>
              </form>
             </div>
           <% end %>       
           </tr>  
           <tr>
            <td colspan="2">
              <a class="list_element" href="#"><%=h event.title %>
              <img id="heart_<%=h event.id %>" src="../images/thumb_up.png"
              <% if event.favorites.find_by_user_id(current_user) %>
              >
              <% else %>
              style="display: none;">
              <% end %>
                <span class="triangle-border">
                   <table cellspacing='0'>
                      <tr>
                         <th>Comments</th>
                      </tr>
                   <% event.comments.each do |comment| %>
                      <tr>
                         <td><%= comment.user.username %> says: "<%= comment.content %>"</td>
                      </tr>
                   <% end %>
                   </table>
                </span>
               </a>   
             </td>
           </tr>
         </table>
       </td>  
     </tr>
   <% end %>
   </table>

