<tr>
  <td><%=h event.title %></td>
  <td><%=h event.description %></td>
  <td><%=h event.place %></td>
  <td><%=h event.start_date %></td>
  <td><%=h event.end_date %></td>

  <td><%=h event.price %></td>
  <td><%=h event.contract_info_name %></td>
  <td><%=h event.contract_info_phone %></td>
  <td><%=h event.contract_info_email %></td>
  <td><%=h event.invitation_type %></td>

  <% if event.pictures.length > 0 then %>
  <td><%= image_tag event.pictures[0].url(:thumb) %></td>
  <% else %>
  <td></td>
  <% end %>
  <td><%= link_to image_tag("../images/page_edit.png", :border=>0, :alt=>"Edit"), edit_event_path(event), :title=> "Edit", :class => 'dialog_form_link' %>
  <td><%= link_to image_tag("../images/bin_closed.png", :border=>0, :alt=>"Delete"), event, :title=> "Delete", :class => 'delete' %>
</tr>

